"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const fc = __importStar(require("fast-check"));
const jest_fast_check_1 = require("jest-fast-check");
const Abi = __importStar(require("@truffle/abi-utils"));
const Example = __importStar(require("../../test/custom-example"));
const compile_abi_1 = require("../../test/compile-abi");
const preflight_1 = require("../../test/preflight");
const _1 = require(".");
const options_1 = require("./options");
const removeProps = (obj, keys) => {
    if (obj instanceof Array) {
        for (const item of obj) {
            removeProps(item, keys);
        }
    }
    else if (typeof obj === "object") {
        for (const [key, value] of Object.entries(obj)) {
            if (keys.has(key)) {
                delete obj[key];
            }
            else {
                removeProps(obj[key], keys);
            }
        }
    }
    return obj;
};
describe("generateSolidity", () => {
    (0, jest_fast_check_1.testProp)("respects output settings", [
        Abi.Arbitrary.Abi(),
        fc.constantFrom(options_1.GenerateSolidityMode.Normal, options_1.GenerateSolidityMode.Embedded),
        fc.boolean(),
        fc.boolean(), // outputSource
    ], (abi, mode, outputAttribution, outputSource) => {
        fc.pre(abi.length > 0);
        const output = (0, _1.generateSolidity)({
            name: "MyInterface",
            abi,
            solidityVersion: "^0.8.20",
            mode,
            outputAttribution,
            outputSource
        });
        const attributionOutput = output.indexOf("abi-to-sol") !== -1;
        const sourceOutput = output.indexOf("FROM THE FOLLOWING ABI JSON:") !== -1;
        const modeOutput = output.indexOf("SPDX-License-Identifier") === -1
            ? options_1.GenerateSolidityMode.Embedded
            : options_1.GenerateSolidityMode.Normal;
        expect(mode).toEqual(modeOutput);
        expect(outputAttribution).toEqual(attributionOutput);
        expect(outputSource).toEqual(sourceOutput);
    });
    (0, jest_fast_check_1.testProp)("compiles to input ABI", [Abi.Arbitrary.Abi()], (abi) => {
        fc.pre(abi.every((entry) => "type" in entry && entry.type !== "constructor"));
        fc.pre((0, preflight_1.excludesFunctionParameters)(abi));
        fc.pre(abi.length > 0);
        const output = (0, _1.generateSolidity)({
            name: "MyInterface",
            abi,
            solidityVersion: "^0.8.20",
        });
        let resultAbi;
        try {
            resultAbi = (0, compile_abi_1.compileAbi)(output);
        }
        catch (error) {
            console.log("Failed to compile. Solidity:\n%s", output);
            throw error;
        }
        const compiledAbi = new Set(removeProps(resultAbi, new Set(["internalType"])));
        const expectedAbi = new Set(Abi.normalize(abi));
        expect(compiledAbi).toEqual(expectedAbi);
    });
    describe("custom example", () => {
        const abiWithoutConstructor = Abi.normalize(Example.abi.filter(({ type }) => type !== "constructor"));
        const output = (0, _1.generateSolidity)({
            name: "Example",
            abi: abiWithoutConstructor,
            solidityVersion: "^0.8.4",
        });
        it("generates output", () => {
            const compiledAbi = (0, compile_abi_1.compileAbi)(output);
            const expectedAbi = abiWithoutConstructor.map((entry) => (Object.assign(Object.assign({}, entry), { type: entry.type || "function" })));
            expect(compiledAbi).toEqual(expectedAbi);
        });
    });
    describe("function pointers", () => {
        const abi = [
            {
                "inputs": [
                    {
                        "internalType": "function (uint256) external returns (uint256)",
                        "name": "f",
                        "type": "function"
                    },
                    {
                        "internalType": "uint256[]",
                        "name": "l",
                        "type": "uint256[]"
                    }
                ],
                "name": "map",
                "outputs": [],
                "stateMutability": "pure",
                "type": "function"
            }
        ];
        const output = (0, _1.generateSolidity)({
            name: "Example",
            abi,
            solidityVersion: "^0.8.13",
        });
        it("generates output", () => {
            const compiledAbi = (0, compile_abi_1.compileAbi)(output);
            expect(compiledAbi).toEqual(abi);
        });
    });
    describe("UDVT support", () => {
        const abi = [
            {
                "inputs": [
                    {
                        "internalType": "Int",
                        "name": "i",
                        "type": "int256"
                    },
                    {
                        "internalType": "Example.Uint",
                        "name": "u",
                        "type": "uint256"
                    },
                    {
                        "internalType": "Other.Bool",
                        "name": "b",
                        "type": "bool"
                    }
                ],
                "name": "fnoo",
                "outputs": [],
                "stateMutability": "pure",
                "type": "function"
            }
        ];
        const output = (0, _1.generateSolidity)({
            name: "Example",
            abi,
            solidityVersion: "^0.8.13",
        });
        it("generates output", () => {
            const compiledAbi = (0, compile_abi_1.compileAbi)(output);
            expect(compiledAbi).toEqual(abi);
        });
    });
});
//# sourceMappingURL=index.test.js.map